pipeline {
  agent any
  
  
  parameters{
    string (name : 'name_branch')
  }
  stages {
    stage('Clear packages') {
      steps {
        sh 'echo Clearing packages en process'
        sh' ls -l'
       
      }
      
    }

  }
}
