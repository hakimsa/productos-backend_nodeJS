pipeline {
  agent any
  stages {
    stage('Clear packages') {
      steps {
        sh '''echo " Clearing packages en process"
Name="kimo"
a=50;
b=50;
echo "this the name :$Name"
echo "total es ${a + b}"
ls
node -version
cat index.js'''
      }
    }

  }
}