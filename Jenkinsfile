pipeline {
  agent any
  stages {
    stage('Clear packages') {
      steps {
        sh '''echo " Clearing packages en process"
Name="kimo"
echo $HOSTNAME
echo "this the name "+$Name
ls
apt update
cat index.js'''
      }
    }

  }
}